version: "3.9"

services:
  backend:
    build: ./Backend
    ports:
      - "8000:8000"
    volumes:
      - backend-data:/data
    env_file:
      - ./Backend/.env
    environment:
      - DB_PATH=/data/unity_master_v3.db
    restart: unless-stopped

volumes:
  backend-data:
